# Credentials — never commit. profiles.yml.example is the tracked template.
profiles.yml
.env
*.p8
*.pem

# dbt
dbt/target/
dbt/dbt_packages/
dbt/logs/
.user.yml

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# Data — the source .xlsx is fetched, not committed
data/raw/*.xlsx
!data/raw/.gitkeep

# Local agent state
.claude/settings.local.json

.DS_Store

# Byproduct of ad-hoc `uv run --with ...`. Dependencies are declared in
# requirements.txt (see AGENTS.md), not managed as a uv project.
uv.lock
